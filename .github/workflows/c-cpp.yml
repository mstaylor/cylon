name: Native C++/Python - gcc,OpenMPI

on:
  push:
    branches:
      - main
      - mirror
      - 0.**
  pull_request:
    branches:
      - main
      - mirror
      - 0.**

jobs:
  build:
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            gcc: 11

    steps:
    - uses: actions/checkout@v4

    # Specify the correct host compilers
    - name: Install/Select gcc and g++
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-${{ matrix.gcc }} g++-${{ matrix.gcc }} autoconf automake libtool libnuma-dev
        echo "CC=/usr/bin/gcc-${{ matrix.gcc }}" >> $GITHUB_ENV
        echo "CXX=/usr/bin/g++-${{ matrix.gcc }}" >> $GITHUB_ENV

    - name: Install Miniconda
      run: |
        wget --quiet https://repo.anaconda.com/miniconda/Miniconda3-py310_23.3.1-0-Linux-x86_64.sh -O ~/miniconda.sh
        bash ~/miniconda.sh -b -p $HOME/miniconda3
        rm ~/miniconda.sh
        echo ". $HOME/miniconda3/etc/profile.d/conda.sh" >> ~/.bash_profile

    - name: Create conda environment
      run: |
        conda install -n base conda-libmamba-solver -y
        conda config --set solver libmamba
        conda env create -f conda/environments/cylon_NoUCX.yml
        echo "conda activate cylon_dev" >> ~/.bash_profile

    - name: Build cylon, pycylon and run cpp test
      run: python build.py -ipath="$HOME/cylon/install" --cpp --python --test

    - name: Run pytest
      run: python build.py -ipath="$HOME/cylon/install" --pytest

    - name: Build Java
      run: python build.py -ipath="$HOME/cylon/install" --java