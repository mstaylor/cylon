[package]
name = "cylon-wasm"
version = "0.1.0"
edition = "2021"
description = "WebAssembly build of Cylon DataFrame operations with SIMD support"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = ["console_error_panic_hook"]
simd = []

[dependencies]
# Use main cylon crate WITHOUT runtime feature (no tokio, socket2, libc)
cylon = { path = "..", default-features = false }

# Arrow (matching cylon's version)
arrow = { version = "57.1.0", default-features = false, features = ["ipc"] }
arrow-array = { version = "57.1.0", default-features = false }
arrow-schema = { version = "57.1.0", default-features = false }
arrow-select = { version = "57.1.0", default-features = false }
arrow-ord = { version = "57.1.0", default-features = false }
arrow-row = { version = "57.1.0", default-features = false }

# WASM bindings
wasm-bindgen = "0.2"
js-sys = "0.3"
web-sys = { version = "0.3", features = ["console"] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Error handling in WASM
console_error_panic_hook = { version = "0.1", optional = true }

# Hashmap for joins
hashbrown = "0.15"

# WASM-compatible random number generation (required by some dependencies)
getrandom = { version = "0.3", features = ["wasm_js"] }

[dev-dependencies]
wasm-bindgen-test = "0.3"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

[profile.release-small]
inherits = "release"
opt-level = "z"
